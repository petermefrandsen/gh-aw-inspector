# Evaluation Report

> NOTE: Produce ONLY the sections listed below and in the exact order. The output MUST be valid Markdown and contain no additional commentary or analysis outside the required sections.

## Description
Provide a concise 1–2 sentence summary of what the workflow does and its primary purpose.

## Metadata
Provide the required metadata as a Markdown table with the exact rows below (leave values blank if unknown):

| Key | Value |
|---|---|
| Required Models | |
| Estimated Duration | |
| Output Format | |

## Prerequisites
A short bulleted list of required setup steps, environment variables, or services. If none, write `None`.

## Intent
One sentence describing the prompt or instruction given to the LLM agent.

## Example Happy Path Output
Provide a representative example of a successful execution output using a fenced code block (triple backticks). Keep the example minimal but representative.

## Example Unhappy Path Outputs
Provide a bulleted list of the most likely failure modes or error messages and a one-line probable cause for each.

---
**Input CLI Output Context:**
{{CLI_OUTPUT}}
