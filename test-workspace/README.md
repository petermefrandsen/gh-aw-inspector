# Test Workspace

This directory is automatically opened when debugging the `gh-aw-inspector` extension. 
It serves as a clean, isolated environment to test the agentic workflows extension against "real" files.

You can create `.md` workflows here and test them using the Extension!
